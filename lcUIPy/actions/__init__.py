# lcUIPy.actions — Python ports of lcUILua/actions/*.lua.
#
# Phase 5 PR-5.7 populates:
#   * remove_operation.py — Python port of removeoperation.lua as a
#                            disabled-by-default proof port + regression
#                            fixture.  Uses `PYREMOVE` as command_line
#                            so it coexists with the Lua `REMOVE` op.
#
# The .lua originals stay authoritative until phase 6 flips the default.
